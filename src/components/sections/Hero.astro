---
import { MessageCircle, Clock } from 'lucide-astro';
import LiquidAction from '../ui/LiquidAction.astro';
---

<section class="hero" aria-label="Estudio gratuito de tu factura de luz" id="hero" itemscope itemtype="https://schema.org/WebPageElement">

  <!-- Floating Cards — premium Liquid Glass dashboard widgets -->
  <div class="card-wrapper" data-card="1" aria-hidden="true">
    <div class="float-card card-1">
      <div class="card-specular"></div>
      <div class="card-inner">
        <div class="card-header">
          <div class="icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true" focusable="false"><path d="M20 6L9 17l-5-5"/></svg>
          </div>
          <div class="card-title-wrap">
            <p class="card-title">Datos clave</p>
            <p class="card-sub">lectura inicial</p>
          </div>
          <div class="data-badge">3</div>
        </div>
        <div class="key-pills" aria-label="Datos clave de la factura">
          <span>Potencia</span>
          <span>kWh</span>
          <span>Extras</span>
        </div>
      </div>
    </div>
  </div>

  <div class="card-wrapper" data-card="2" aria-hidden="true">
    <div class="float-card card-2">
      <div class="card-specular"></div>
      <div class="card-inner">
        <div class="card-header">
          <div class="icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true" focusable="false"><path d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
          </div>
          <div class="card-title-wrap">
            <p class="card-title">Coste por kWh</p>
            <p class="card-sub">factura actual</p>
          </div>
        </div>
        <div class="comparison-chart">
          <div class="comp-row">
            <span class="comp-label">Factura</span>
            <div class="comp-bar-wrap"><div class="comp-bar comp-bar-old"></div></div>
            <span class="comp-val">0,178€</span>
          </div>
          <div class="comp-row">
            <span class="comp-label accent">Opción revisada</span>
            <div class="comp-bar-wrap"><div class="comp-bar comp-bar-new"></div></div>
            <span class="comp-val accent">0,151€</span>
          </div>
        </div>
        <div class="comp-footer">
          <span class="savings-pill">Comparación sin compromiso</span>
        </div>
      </div>
    </div>
  </div>

  <div class="card-wrapper" data-card="3" aria-hidden="true">
    <div class="float-card card-3">
      <div class="card-specular"></div>
      <div class="card-inner">
        <div class="card-header">
          <div class="icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true" focusable="false"><path d="M13 2 3 14h8l-1 8 11-14h-8z"/></svg>
          </div>
          <div class="card-title-wrap">
            <p class="card-title">Suministro intacto</p>
            <p class="card-sub">sin cortes ni obra</p>
          </div>
        </div>
        <div class="supply-shield" aria-label="Garantía de suministro">
          <div class="shield-ring">
            <div class="shield-core">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true" focusable="false"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
            </div>
          </div>
          <div class="shield-copy">
            <strong>Suministro protegido</strong>
            <span>Sin cortes ni cambios físicos</span>
          </div>
          <div class="shield-meta">
            <span class="meta-pill">Mismo contador</span>
            <span class="meta-pill">Misma instalación</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Hero content -->
  <div class="content">

    <div class="reveal stagger-1">
      <div class="eyebrow">
        <span class="eyebrow-dot"></span>
        <span class="eyebrow-text">Revisión energética personal</span>
        <span class="eyebrow-sep"></span>
        <span class="eyebrow-free">
          <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M20 6L9 17l-5-5"/></svg>
          Sin coste inicial
        </span>
      </div>
    </div>

    <h1 class="headline reveal stagger-2" itemprop="headline">
      <span class="line line-1">Reviso tu factura.</span>
      <span class="line line-accent">Tú decides.</span>
    </h1>

    <p class="subhead reveal stagger-3">
      Analizo consumo, tarifa y condiciones para explicarte si hay una opción que aporte ahorro real.
      <strong>Solo se tramita con tu autorización.</strong>
    </p>

    <div class="cta-group reveal stagger-4">
      <LiquidAction
        href="#contacto"
        class="cta-btn"
        ariaLabel="Solicitar revisión gratuita de factura"
        bgClass="cta-btn-bg"
      >
        <MessageCircle size={20} fill="currentColor" style="position:relative;z-index:1" aria-hidden="true" />
        <span style="position:relative;z-index:1">Solicitar revisión gratuita</span>
      </LiquidAction>
      <p class="cta-note">
        <Clock size={12} aria-hidden="true" />
        Respuesta habitual en 24 h
      </p>
    </div>

    <div class="trust reveal stagger-5">
      <span class="pill">
        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true" focusable="false"><path d="M20 6L9 17l-5-5"/></svg>
        Cambio solo autorizado
      </span>
      <span class="pill">
        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true" focusable="false"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        Datos solo para revisar
      </span>
      <span class="pill">
        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true" focusable="false"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
        Sin presión comercial
      </span>
      <span class="pill pill-gold">
        <svg width="11" height="11" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" focusable="false"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
        Estudio inicial gratuito
      </span>
    </div>
  </div>

</section>

<style>
  /* ─── Hero shell ─────────────────────────────────────────────── */
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 96px var(--space-6);
    overflow: hidden;
    /* No background - using global background from Layout */
  }

  /* ─── Floating Cards ─────────────────────────────────────────── */
  .card-wrapper {
    position: absolute;
    z-index: 2;
    display: none;
    will-change: transform;
    transition: transform var(--duration-normal) var(--ease-ios);
  }
  [data-card="1"] { left: 4%; top: 26%; }
  [data-card="2"] { right: 4%; top: 22%; }
  [data-card="3"] { left: 5%; top: 56%; }
  @media (min-width: 1100px) { .card-wrapper { display: block; } }

  .card-wrapper:hover {
    z-index: 10;
  }

  .card-wrapper:hover .float-card {
    transform: translateY(-7px) scale(1.012);
    box-shadow: var(--shadow-glass-hover);
  }

  .float-card {
    width: 260px;
    border-radius: var(--radius-xl);
    overflow: hidden;
    background: var(--glass-bg);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    border: 1px solid var(--glass-border);
    box-shadow: var(--shadow-glass);
    transition: transform 0.5s var(--ease-ios-spring), box-shadow 0.4s var(--ease-ios), background 0.35s var(--ease-ios);
    will-change: transform;
    position: relative;
  }

  .float-card::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: var(--radius-xl);
    background: linear-gradient(135deg, rgba(255,255,255,0.3) 0%, transparent 50%);
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.4s var(--ease-ios);
  }

  .card-wrapper:hover .float-card::after {
    opacity: 1;
  }

  /* Color tint per card — ultra-subtle */
  .card-1 { background: rgba(245, 250, 255, 0.72); }
  .card-2 { background: rgba(245, 250, 255, 0.72); }
  .card-3 { background: rgba(245, 250, 255, 0.72); }

  /* Individual float animations */
  .card-1 {
    animation: float1 6s ease-in-out infinite, cardEnter 1s var(--ease-ios-decel) forwards;
    animation-delay: 0s, 0.6s;
    opacity: 0;
  }
  .card-2 {
    animation: float2 7s ease-in-out infinite, cardEnter 1s var(--ease-ios-decel) forwards;
    animation-delay: -2s, 0.75s;
    opacity: 0;
  }
  .card-3 {
    animation: float3 8s ease-in-out infinite, cardEnter 1s var(--ease-ios-decel) forwards;
    animation-delay: -4s, 0.9s;
    opacity: 0;
  }

  @keyframes cardEnter {
    from {
      opacity: 0;
      transform: translateY(30px) scale(0.95);
      filter: blur(4px);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
      filter: blur(0);
    }
  }

  @keyframes float1 {
    0%, 100% { transform: translateY(0) translateX(0); }
    25% { transform: translateY(-6px) translateX(3px); }
    50% { transform: translateY(-3px) translateX(-2px); }
    75% { transform: translateY(-8px) translateX(2px); }
  }

  @keyframes float2 {
    0%, 100% { transform: translateY(0) translateX(0); }
    33% { transform: translateY(-8px) translateX(-3px); }
    66% { transform: translateY(-4px) translateX(2px); }
  }

  @keyframes float3 {
    0%, 100% { transform: translateY(0) translateX(0); }
    20% { transform: translateY(-5px) translateX(2px); }
    40% { transform: translateY(-9px) translateX(-2px); }
    60% { transform: translateY(-4px) translateX(3px); }
    80% { transform: translateY(-7px) translateX(-1px); }
  }

  /* Specular highlight */
  .card-specular {
    position: absolute;
    inset: 0 0 auto;
    height: 55%;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.92) 0%,
      rgba(255, 255, 255, 0.45) 35%,
      rgba(255, 255, 255, 0.08) 65%,
      transparent 100%
    );
    pointer-events: none;
    z-index: 1;
    opacity: 0.85;
  }

  /* Card content */
  .card-inner {
    position: relative;
    z-index: 2;
    padding: var(--space-4);
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    margin-bottom: 10px;
  }

  .icon {
    width: 36px; height: 36px;
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    position: relative;
    overflow: hidden;
    background: linear-gradient(135deg, rgba(0,122,255,0.12) 0%, rgba(0,122,255,0.06) 100%);
    backdrop-filter: blur(10px) saturate(160%);
    -webkit-backdrop-filter: blur(10px) saturate(160%);
    border: 1px solid rgba(0,122,255,0.18);
    color: var(--color-primary);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.65),
      0 2px 6px rgba(0,122,255,0.12),
      0 4px 12px rgba(0,122,255,0.06);
    transition: all var(--duration-normal) var(--ease-ios-spring);
  }

  .icon::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: linear-gradient(135deg, rgba(255,255,255,0.55) 0%, transparent 50%);
    pointer-events: none;
  }

  /* Card-3 icon tint: purple/indigo */
  .card-3 .icon {
    background: linear-gradient(135deg, rgba(88,86,214,0.12) 0%, rgba(88,86,214,0.06) 100%);
    border-color: rgba(88,86,214,0.18);
    color: var(--ios-indigo);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.65),
      0 2px 6px rgba(88,86,214,0.12),
      0 4px 12px rgba(88,86,214,0.06);
  }

  .card-wrapper:hover .icon {
    transform: scale(1.12) rotate(-3deg);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.85),
      0 4px 14px rgba(0,122,255,0.22),
      0 8px 24px rgba(0,122,255,0.12);
    border-color: rgba(0,122,255,0.28);
  }

  .card-wrapper:hover .card-3 .icon {
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.85),
      0 4px 14px rgba(88,86,214,0.22),
      0 8px 24px rgba(88,86,214,0.12);
    border-color: rgba(88,86,214,0.28);
  }

  .card-wrapper:hover .icon svg {
    animation: icon-bounce 0.5s var(--ease-ios-spring);
  }

  @keyframes icon-bounce {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.15); }
  }

  @media (prefers-reduced-motion: reduce) {
    .card-1,
    .card-2,
    .card-3,
    .cta-btn-bg {
      animation: none !important;
      opacity: 1;
    }

    .card-wrapper:hover .float-card,
    .card-wrapper:hover .icon,
    .card-wrapper:hover .icon svg {
      transform: none;
    }
  }

  .card-title-wrap { flex: 1; min-width: 0; }

  .card-title {
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    color: var(--text-primary);
    margin: 0;
    line-height: var(--leading-none);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .card-sub {
    font-size: var(--text-xs);
    color: var(--text-quaternary);
    margin: 2px 0 0;
  }

  /* Card-1: Key data rows — ultra-minimal */
  .data-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 20px;
    height: 18px;
    padding: 0 5px;
    border-radius: var(--radius-pill);
    color: var(--color-primary);
    background: rgba(0, 122, 255, 0.08);
    border: 1px solid rgba(0, 122, 255, 0.14);
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.50);
    font-size: 0.60rem;
    font-weight: var(--font-bold);
    font-variant-numeric: tabular-nums;
    flex-shrink: 0;
    letter-spacing: 0.02em;
  }

  /* Card-1: Key pills — clean glass tags */
  .key-pills {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  .key-pills span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 7px 14px;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.45);
    border: 1px solid rgba(255, 255, 255, 0.60);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.70);
    font-size: 0.74rem;
    font-weight: var(--font-semibold);
    color: var(--text-secondary);
    line-height: 1;
    white-space: nowrap;
    transition: all 0.25s var(--ease-ios);
  }

  .card-wrapper:hover .key-pills span {
    background: rgba(255, 255, 255, 0.65);
    color: var(--text-primary);
    transform: translateY(-1px);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.85), 0 3px 10px rgba(0,0,0,0.04);
  }

  /* Card-2: Comparison chart */
  .comparison-chart { margin-bottom: var(--space-3); }

  .comp-row {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 10px;
  }
  .comp-row:last-child { margin-bottom: 0; }

  .comp-label {
    font-size: var(--text-xs);
    color: var(--text-quaternary);
    width: 64px;
    flex-shrink: 0;
  }
  .comp-label.accent { color: var(--color-primary); font-weight: var(--font-semibold); }

  .comp-bar-wrap {
    flex: 1;
    height: 7px;
    background: rgba(0,0,0,0.04);
    border-radius: var(--radius-pill);
    overflow: hidden;
    position: relative;
    box-shadow: inset 0 1px 2px rgba(0,0,0,0.05);
  }

  .comp-bar {
    height: 100%;
    border-radius: var(--radius-pill);
    position: relative;
    box-shadow: 0 1px 2px rgba(0,0,0,0.06);
  }

  .comp-bar::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(180deg, rgba(255,255,255,0.30) 0%, transparent 100%);
    border-radius: var(--radius-pill) var(--radius-pill) 0 0;
  }

  .comp-bar-old {
    width: 100%;
    background: linear-gradient(180deg, rgba(0,0,0,0.10) 0%, rgba(0,0,0,0.05) 100%);
  }

  .comp-bar-new {
    width: 78%;
    background: linear-gradient(90deg, var(--blue-600) 0%, var(--color-primary) 50%, var(--cyan-400) 100%);
    animation: barGrow 1.2s ease-out forwards;
    transform-origin: left;
  }

  @keyframes barGrow {
    from { transform: scaleX(0); }
    to { transform: scaleX(1); }
  }

  .comp-val {
    font-size: var(--text-xs);
    color: var(--text-tertiary);
    width: 58px;
    text-align: right;
    flex-shrink: 0;
    font-variant-numeric: tabular-nums;
  }
  .comp-val.accent { color: var(--color-primary); font-weight: var(--font-bold); }

  .comp-footer {
    display: flex;
    justify-content: flex-start;
  }

  .savings-pill {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    background: linear-gradient(135deg, rgba(0,122,255,0.12) 0%, rgba(0,122,255,0.05) 100%);
    backdrop-filter: blur(14px) saturate(160%);
    -webkit-backdrop-filter: blur(14px) saturate(160%);
    color: var(--color-primary);
    max-width: 100%;
    font-size: 0.6875rem;
    font-weight: var(--font-semibold);
    line-height: 1.2;
    padding: 5px 10px;
    border-radius: var(--radius-pill);
    border: 1px solid rgba(0,122,255,0.18);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.55),
      0 2px 6px rgba(0,122,255,0.10);
    position: relative;
    overflow: hidden;
    transition: all var(--duration-normal) var(--ease-ios-spring);
  }

  .savings-pill::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(180deg, rgba(255,255,255,0.45) 0%, transparent 100%);
    border-radius: var(--radius-pill) var(--radius-pill) 0 0;
    pointer-events: none;
  }

  .card-wrapper:hover .savings-pill {
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.65),
      0 3px 10px rgba(0,122,255,0.12);
  }

  /* Card-3: Shield status — premium glass emblem */
  .supply-shield {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 14px;
    padding: 10px 6px;
  }

  .shield-ring {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    background: linear-gradient(135deg, rgba(52,199,89,0.18) 0%, rgba(0,122,255,0.10) 100%);
    border: 1px solid rgba(255, 255, 255, 0.70);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.85),
      0 8px 24px rgba(52,199,89,0.14),
      0 4px 12px rgba(0,122,255,0.08);
    transition: all 0.4s var(--ease-ios-spring);
  }

  .card-wrapper:hover .shield-ring {
    transform: scale(1.06);
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.95),
      0 12px 32px rgba(52,199,89,0.20),
      0 6px 16px rgba(0,122,255,0.12);
  }

  .shield-core {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--ios-green) 0%, #30D158 50%, var(--ios-blue) 130%);
    color: white;
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.40), 0 4px 14px rgba(52,199,89,0.25);
    position: relative;
    overflow: hidden;
  }

  .shield-core::before {
    content: '';
    position: absolute;
    inset: 0 0 auto;
    height: 50%;
    background: linear-gradient(180deg, rgba(255,255,255,0.35) 0%, transparent 100%);
    pointer-events: none;
    border-radius: 50% 50% 0 0;
  }

  .shield-core svg {
    position: relative;
    z-index: 1;
    stroke-width: 2.8;
  }

  .shield-copy {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3px;
    text-align: center;
  }

  .shield-copy strong {
    font-size: 0.82rem;
    font-weight: var(--font-bold);
    color: var(--text-primary);
    line-height: 1.2;
    letter-spacing: -0.01em;
  }

  .shield-copy span {
    font-size: 0.68rem;
    font-weight: var(--font-medium);
    color: var(--text-tertiary);
    line-height: 1.2;
  }

  .shield-meta {
    display: flex;
    gap: 6px;
    justify-content: center;
    width: 100%;
  }

  .meta-pill {
    display: inline-flex;
    align-items: center;
    padding: 4px 10px;
    border-radius: var(--radius-pill);
    background: rgba(255, 255, 255, 0.45);
    border: 1px solid rgba(255, 255, 255, 0.58);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.70);
    font-size: 0.62rem;
    font-weight: var(--font-semibold);
    color: var(--text-secondary);
    line-height: 1;
    white-space: nowrap;
    transition: all 0.25s var(--ease-ios);
  }

  .card-wrapper:hover .meta-pill {
    background: rgba(255, 255, 255, 0.62);
    color: var(--text-primary);
  }

  /* ─── Hero content ───────────────────────────────────────────── */
  .content {
    position: relative;
    z-index: 10;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 760px;
    margin-top: clamp(2.5rem, 6vh, 4.75rem);
    padding: 0 var(--space-4);
  }

  /* Eyebrow pill - Liquid Glass Formula (matches trust pills) */
  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-2) var(--space-5);
    border-radius: var(--radius-pill);
    margin-bottom: var(--space-8);
    background: rgba(255, 255, 255, 0.70);
    backdrop-filter: blur(40px) saturate(200%) brightness(1.03);
    -webkit-backdrop-filter: blur(40px) saturate(200%) brightness(1.03);
    border: 1px solid rgba(255, 255, 255, 0.58);
    box-shadow: var(--shadow-glass);
    opacity: 0;
    transform: translateY(10px) scale(0.96);
    animation: fadeUpScale 0.7s var(--ease-ios-decel) forwards;
    animation-delay: 0.05s;
    position: relative;
    overflow: hidden;
    transition: all var(--duration-normal) var(--ease-ios-spring);
  }

  /* Specular highlight - matches pills */
  .eyebrow::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 55%;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.88) 0%,
      rgba(255, 255, 255, 0.40) 35%,
      rgba(255, 255, 255, 0.08) 65%,
      transparent 100%
    );
    pointer-events: none;
    border-radius: var(--radius-pill) var(--radius-pill) 0 0;
    z-index: 1;
  }

  .eyebrow:hover {
    transform: translateY(-2px) scale(1.02);
    background: rgba(255, 255, 255, 0.80);
    box-shadow: var(--shadow-glass-hover);
  }

  @keyframes fadeUpScale {
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  .eyebrow-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--cyan-400) 100%);
    flex-shrink: 0;
    position: relative;
    box-shadow:
      inset 0 1px 0 rgba(255,255,255,0.50),
      0 2px 6px rgba(0,122,255,0.35);
    animation: pulse-blue 2.2s ease-in-out infinite;
  }

  @keyframes pulse-blue {
    0%, 100% { box-shadow: inset 0 1px 0 rgba(255,255,255,0.50), 0 2px 6px rgba(0,122,255,0.35); }
    50% { box-shadow: inset 0 1px 0 rgba(255,255,255,0.50), 0 2px 12px rgba(0,122,255,0.55); }
  }

  .eyebrow-dot::before {
    content: '';
    position: absolute;
    top: 1px; left: 1px; right: 1px; bottom: 1px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(255,255,255,0.55) 0%, transparent 50%);
  }

  .eyebrow-dot,
  .eyebrow-text,
  .eyebrow-sep,
  .eyebrow-free {
    position: relative;
    z-index: 2;
  }

  .eyebrow-text {
    font-size: var(--text-base);
    color: var(--text-secondary);
    font-weight: var(--font-medium);
  }

  .eyebrow-sep {
    width: 1px; height: 14px;
    background: rgba(0,0,0,0.07);
    flex-shrink: 0;
  }

  .eyebrow-free {
    display: flex;
    align-items: center;
    gap: 5px;
    font-size: var(--text-base);
    font-weight: var(--font-bold);
    color: var(--color-primary);
  }
  .eyebrow-free svg { stroke: var(--color-primary); }

  /* ─── Headline ──────────────────────────────────────────── */
  .headline {
    margin: 0 0 var(--space-6);
    text-align: center;
  }

  .line {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(2.45rem, 6.3vw, 4.9rem);
    font-weight: var(--font-extrabold);
    letter-spacing: var(--tracking-tight);
    line-height: 1.05;
    text-wrap: balance;
    overflow-wrap: break-word;
    opacity: 0;
    transform: translateY(18px);
    animation: fadeUp 0.8s var(--ease-ios-decel) forwards;
  }

  .line-1 {
    color: var(--text-primary);
    animation-delay: 0.1s;
  }

  .line-accent {
    background: linear-gradient(135deg, var(--blue-600) 0%, var(--blue-500) 30%, var(--cyan-400) 50%, var(--blue-500) 70%, var(--blue-600) 100%);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: fadeUp 0.8s var(--ease-ios-decel) forwards, gradient-shift 5s ease infinite;
    animation-delay: 0.25s, 1s;
  }

  @keyframes gradient-shift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  @keyframes fadeUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* ─── Subhead ──────────────────────────────────────────────── */
  .subhead {
    font-size: clamp(1.0625rem, 2.2vw, 1.25rem);
    line-height: 1.55;
    color: var(--text-secondary);
    letter-spacing: var(--tracking-normal);
    margin: 0 0 var(--space-8);
    max-width: 440px;
    opacity: 0;
    transform: translateY(12px);
    animation: fadeUp 0.6s var(--ease-ios-decel) forwards;
    animation-delay: 0.35s;
  }

  .subhead strong {
    color: var(--text-primary);
    font-weight: var(--font-semibold);
  }

  /* ─── CTA ───────────────────────────────────────────────────── */
  .cta-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
    opacity: 0;
    transform: translateY(12px);
    animation: fadeUp 0.6s var(--ease-ios-decel) forwards;
    animation-delay: 0.45s;
  }

  :global(.cta-btn) {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: 17px 36px;
    border-radius: var(--radius-pill);
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    font-family: var(--font-display);
    color: var(--text-inverse);
    text-decoration: none;
    overflow: hidden;
    letter-spacing: var(--tracking-normal);
    transition: transform var(--duration-normal) var(--ease-ios-spring), box-shadow var(--duration-normal) var(--ease-ios);
    box-shadow:
      0 4px 14px rgba(0,122,255,0.32),
      0 1px 3px rgba(0,122,255,0.18),
      inset 0 1px 0 rgba(255,255,255,0.22);
    will-change: transform;
    touch-action: manipulation;
    -webkit-tap-highlight-color: transparent;
  }

  :global(.cta-btn:focus-visible) {
    outline: 2px solid white;
    outline-offset: 3px;
    box-shadow: 0 0 0 5px rgba(0, 122, 255, 0.4);
  }

  /* Animated gradient background */
  :global(.cta-btn-bg) {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-hover) 50%, var(--color-primary) 100%);
    background-size: 200% 100%;
    animation: shimmer-btn 4.5s ease-in-out infinite;
  }

  /* Glossy shine overlay */
  :global(.cta-btn::before) {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 50%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255,255,255,0.22),
      transparent
    );
    animation: shine 3.5s ease-in-out infinite;
  }

  @keyframes shimmer-btn {
    0%, 100% { background-position: 0% 50%; }
    50%       { background-position: 100% 50%; }
  }

  @keyframes shine {
    0%, 100% { left: -100%; }
    50% { left: 150%; }
  }

  :global(.cta-btn:hover) {
    transform: scale(1.04) translateY(-3px);
    box-shadow:
      0 8px 28px rgba(0,122,255,0.45),
      0 4px 14px rgba(0,122,255,0.25),
      inset 0 1px 0 rgba(255,255,255,0.35);
    letter-spacing: 0.02em;
  }

  :global(.cta-btn:hover .cta-btn-bg) {
    animation-duration: 2s;
  }

  :global(.cta-btn:active) {
    transform: scale(0.97) translateY(0);
    box-shadow: 0 3px 12px rgba(0,122,255,0.32);
    transition-duration: 0.1s;
  }

  .cta-note {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    font-size: var(--text-sm);
    color: var(--text-tertiary);
    margin: var(--space-3) 0 0;
  }
  .cta-note svg { stroke: var(--color-primary); }

  /* ─── Trust pills ────────────────────────────────────────────── */
  .trust {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    justify-content: center;
    margin-top: var(--space-8);
    opacity: 0;
    transform: translateY(12px);
    animation: fadeUp 0.6s var(--ease-ios-decel) forwards;
    animation-delay: 0.5s;
  }

  .pill {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 10px 18px;
    border-radius: 100px;
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    color: var(--text-secondary);
    /* Liquid Glass Formula */
    background: rgba(255, 255, 255, 0.70);
    backdrop-filter: blur(40px) saturate(200%) brightness(1.03);
    -webkit-backdrop-filter: blur(40px) saturate(200%) brightness(1.03);
    border: 1px solid rgba(255, 255, 255, 0.58);
    box-shadow: var(--shadow-glass);
    transition: all var(--duration-normal) var(--ease-ios-spring);
    overflow: hidden;
  }

  /* Specular highlight - top layer */
  .pill::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 55%;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.88) 0%,
      rgba(255, 255, 255, 0.40) 35%,
      rgba(255, 255, 255, 0.08) 65%,
      transparent 100%
    );
    pointer-events: none;
    border-radius: 100px 100px 0 0;
    z-index: 1;
  }

  .pill > * {
    position: relative;
    z-index: 2;
  }

  .pill:hover {
    transform: translateY(-2px) scale(1.02);
    background: rgba(255, 255, 255, 0.80);
    box-shadow: var(--shadow-glass-hover);
  }

  .pill svg { 
    stroke: var(--text-tertiary); 
    flex-shrink: 0;
  }

  .pill-gold {
    color: var(--color-primary);
    background: rgba(236, 246, 255, 0.72);
    border-color: rgba(0, 122, 255, 0.25);
    box-shadow: var(--shadow-glass);
  }

  .pill-gold::before {
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.95) 0%,
      rgba(255, 255, 255, 0.50) 35%,
      rgba(255, 255, 255, 0.10) 65%,
      transparent 100%
    );
  }

  .pill-gold:hover {
    background: rgba(236, 246, 255, 0.85);
    box-shadow: var(--shadow-glass-hover);
  }

  .pill-gold svg { 
    fill: var(--color-primary); 
    stroke: none;
  }

  /* ─── Responsive ─────────────────────────────────────────────── */
  @media (max-width: 1199px) and (min-width: 992px) {
    .float-card { display: none; }
    .hero { padding: 96px var(--space-5); }
  }

  @media (max-width: 991px) and (min-width: 768px) {
    .float-card { display: none; }
    .hero { padding: 96px var(--space-5); }
    .headline { margin-bottom: var(--space-5); }
    .subhead { margin-bottom: var(--space-6); max-width: 400px; }
    .trust { margin-top: var(--space-6); }
  }

  @media (max-width: 767px) {
    .hero { padding: 88px var(--space-4); }
    .float-card { display: none; }
    .content {
      margin-top: clamp(1.5rem, 4vh, 2.5rem);
      padding: 0 var(--space-3);
    }

    .line {
      font-size: clamp(2rem, 8.8vw, 2.9rem);
      letter-spacing: var(--tracking-tighter);
    }

    .eyebrow {
      padding: var(--space-2) var(--space-4);
      margin-bottom: var(--space-6);
      gap: var(--space-2);
      max-width: 100%;
    }
    .eyebrow-text, .eyebrow-sep { display: none; }
    .eyebrow-free { font-size: var(--text-sm); }

    .subhead {
      font-size: var(--text-md);
      line-height: 1.5;
      margin-bottom: var(--space-6);
      max-width: 340px;
    }

    :global(.cta-btn) {
      padding: 15px 28px;
      font-size: var(--text-md);
      width: 100%;
      justify-content: center;
    }

    .cta-note {
      font-size: var(--text-sm);
      margin-top: var(--space-2);
    }

    .trust {
      margin-top: var(--space-6);
      gap: var(--space-1);
    }
    .pill {
      font-size: var(--text-sm);
      padding: 8px 14px;
    }
  }

  @media (max-width: 480px) {
    .hero { padding: 80px max(var(--space-3), env(safe-area-inset-left)) 48px max(var(--space-3), env(safe-area-inset-right)); }
    .content { margin-top: 1.75rem; }
    .line { font-size: clamp(1.85rem, 8.5vw, 2.6rem); }
    .subhead { font-size: var(--text-base); max-width: 100%; padding: 0 var(--space-1); }
    :global(.cta-btn) { padding: 14px var(--space-5); font-size: var(--text-base); }
    .trust { gap: 4px; margin-top: var(--space-5); }
    .pill { font-size: 0.75rem; padding: 7px 11px; }
    .eyebrow { padding: var(--space-1) var(--space-3); gap: var(--space-1); flex-wrap: wrap; justify-content: center; }
    .eyebrow-free { font-size: var(--text-sm); gap: 3px; }
  }

  @media (max-width: 360px) {
    .hero { padding-top: 76px; }
    .line { font-size: 1.75rem; }
    .subhead { font-size: var(--text-sm); }
    :global(.cta-btn) { padding: 13px var(--space-4); }
  }

  /* ─── Reduced Motion ─────────────────────────────────────────── */
  @media (prefers-reduced-motion: reduce) {
    .orb,
    :global(.cta-btn-bg),
    :global(.cta-btn::before),
    .line-accent,
    .eyebrow {
      animation: none !important;
    }
    .line,
    .subhead,
    .cta-group,
    .trust,
    .eyebrow {
      opacity: 1 !important;
      transform: none !important;
    }
    .float-card {
      opacity: 1;
      animation: none !important;
    }
    .comp-bar-new { animation: none !important; width: 78%; }
  }
</style>

<script>
  // Parallax effect on mouse move
  const hero = document.getElementById('hero');
  const cardWrappers = document.querySelectorAll('.card-wrapper');
  let rafId: number | null = null;
  let mouseX = 0;
  let mouseY = 0;
  let currentX = 0;
  let currentY = 0;

  const noMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (hero && !window.matchMedia('(pointer: coarse)').matches && !noMotion && cardWrappers.length > 0) {
    hero.addEventListener('mousemove', (e) => {
      const rect = hero.getBoundingClientRect();
      mouseX = (e.clientX - rect.left) / rect.width - 0.5;
      mouseY = (e.clientY - rect.top) / rect.height - 0.5;

      if (!rafId) {
        rafId = requestAnimationFrame(() => {
          currentX += (mouseX - currentX) * 0.08;
          currentY += (mouseY - currentY) * 0.08;

          cardWrappers.forEach((wrapper, index) => {
            const depth = (index + 1) * 4;
            const moveX = currentX * depth * -1;
            const moveY = currentY * depth * -1;
            (wrapper as HTMLElement).style.transform = `translate(${moveX}px, ${moveY}px)`;
          });
          rafId = null;
        });
      }
    }, { passive: true });
  }
</script>
