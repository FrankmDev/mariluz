---
import { Image } from 'astro:assets';
import { siteConfig, faqData } from '../../config/site';
import aboutPhoto from '../../assets/about.avif';
import { MessageCircle, Phone } from 'lucide-astro';
import SectionHeader from '../ui/SectionHeader.astro';
import LiquidAction from '../ui/LiquidAction.astro';

const faqs = faqData.map(faq => ({
  question: faq.question,
  answer: faq.answer,
}));
---

<section id="faq" class="faq-section">
  <div class="container-custom faq-container">

    <SectionHeader
      eyebrow="Preguntas frecuentes"
      title="Preguntas clave."
      subtitle="Información clara antes de solicitar la revisión."
    />

    <div class="faq-layout">

      <!-- Left: FAQ Accordion with Liquid Glass -->
      <div class="faq-col reveal" style="--delay: 0ms;">
        <div class="faq-list">
          {faqs.map((faq, index) => (
            <details class="faq-item" data-faq={index} style={`--faq-index: ${index}`}>
              <summary class="faq-summary">
                <div class="faq-summary-content">
                  <span class="faq-number">{String(index + 1).padStart(2, '0')}</span>
                  <span class="faq-question">{faq.question}</span>
                </div>
                <div class="faq-icon-btn">
                  <div class="faq-icon-bg">
                    <svg class="faq-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                      <path d="M6 9l6 6 6-6"/>
                    </svg>
                  </div>
                </div>
              </summary>
              <div class="faq-body">
                <div class="faq-body-inner">
                  <div class="faq-answer">
                    <p>{faq.answer}</p>
                  </div>
                </div>
              </div>
            </details>
          ))}
        </div>
      </div>

      <!-- Right: Contact Card -->
      <div class="faq-contact-col reveal" style="--delay: 150ms;">

        <div class="contact-card" data-contact-card>
          <!-- Specular Highlight Layer -->
          <div class="contact-specular"></div>

          <!-- Card Content -->
          <div class="contact-card-content">
            <!-- Avatar -->
            <div class="contact-avatar-wrap">
              <div class="contact-avatar">
                <Image
                  src={aboutPhoto}
                   alt="Mariluz Marín, asesora energética en Zoco Energía"
                  width={88}
                  height={88}
                  loading="lazy"
                  decoding="async"
                />
              </div>
              <div class="contact-status">
                <span class="status-ping"></span>
              </div>
            </div>

            <h3 class="contact-title">¿Necesitas aclararlo?</h3>
            <p class="contact-description">
              Puedes consultar cualquier detalle antes de enviar la factura o autorizar un cambio.
            </p>

            <!-- CTAs -->
            <div class="contact-actions">
              <LiquidAction
                href={siteConfig.whatsappUrl}
                class="contact-cta contact-cta-primary"
                target="_blank"
                rel="noopener noreferrer"
                bgClass="cta-bg"
                contentClass="cta-content"
              >
                <MessageCircle size={18} fill="currentColor" aria-hidden="true" />
                Escribir por WhatsApp
              </LiquidAction>

              <LiquidAction href={`tel:${siteConfig.phone.replace(/\s/g, '')}`} class="contact-cta contact-cta-secondary">
                <Phone size={18} aria-hidden="true" />
                {siteConfig.phone}
              </LiquidAction>
            </div>

            <!-- Trust indicators -->
            <div class="contact-trust">
              <div class="trust-item">
                <div class="trust-icon">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20 6L9 17l-5-5"/>
                  </svg>
                </div>
                <span class="text-caption">Sin cambios sin autorización</span>
              </div>
              <div class="trust-item">
                <div class="trust-icon">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20 6L9 17l-5-5"/>
                  </svg>
                </div>
                <span class="text-caption">Condiciones explicadas antes</span>
              </div>
              <div class="trust-item">
                <div class="trust-icon">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20 6L9 17l-5-5"/>
                  </svg>
                </div>
                <span class="text-caption">Revisión inicial sin coste</span>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>

  </div>
</section>

<style>
  /* ========== FAQ Section Base ========== */
  .faq-section {
    position: relative;
    padding: var(--section-py) 0;
    overflow: hidden;
    /* No background - using global background from Layout */
  }

  .faq-container {
    position: relative;
    z-index: 1;
  }

  /* ========== FAQ Layout ========== */
  .faq-layout {
    display: grid;
    grid-template-columns: 1.6fr 1fr;
    gap: var(--space-20);
    align-items: start;
  }

  /* ========== FAQ List with Liquid Glass Cards ========== */
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .faq-item {
    position: relative;
    display: block;
    background: var(--glass-bg);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-glass);
    overflow: hidden;
    transition: transform 0.45s var(--ease-ios-spring), background 0.35s var(--ease-ios), box-shadow 0.35s var(--ease-ios);
    transform: translateZ(0);
    will-change: transform;
  }

  .faq-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 55%;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.92) 0%,
      rgba(255, 255, 255, 0.45) 35%,
      rgba(255, 255, 255, 0.08) 65%,
      transparent 100%
    );
    pointer-events: none;
    z-index: 1;
    border-radius: var(--radius-xl) var(--radius-xl) 0 0;
  }

  .faq-item::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 1;
    border-radius: inherit;
    background:
      linear-gradient(120deg, transparent 0%, rgba(255,255,255,0.18) 20%, transparent 38%),
      radial-gradient(ellipse 78% 46% at 50% -10%, rgba(0,122,255,0.08), transparent 72%);
    opacity: 0;
    transform: translateX(-14%);
    pointer-events: none;
    transition: opacity 0.35s var(--ease-ios), transform 0.7s var(--ease-ios-decel);
  }

  .faq-item:hover {
    background: var(--glass-bg-hover);
    transform: translateY(-3px) scale(1.002);
    box-shadow: var(--shadow-glass-hover);
  }

  .faq-item:hover::after {
    opacity: 0.72;
    transform: translateX(0);
  }

  .faq-item:hover .faq-number {
    color: var(--color-primary);
    transform: scale(1.1);
  }

  .faq-number {
    transition: color 0.3s ease, transform 0.3s var(--ease-ios-spring);
  }

  .faq-item.is-open {
    background: rgba(255, 255, 255, 0.86);
    box-shadow: var(--shadow-glass-hover);
    transform: translateY(-2px) scale(1.002);
  }

  .faq-item.is-open:hover {
    transform: translateY(-2px);
  }

  .faq-summary {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-6);
    cursor: pointer;
    list-style: none;
    user-select: none;
    -webkit-user-select: none;
    gap: var(--space-4);
    transition: color 0.3s ease;
  }

  .faq-summary:focus-visible {
    outline: 2px solid rgba(0, 122, 255, 0.72);
    outline-offset: -5px;
    border-radius: var(--radius-xl);
  }

  .faq-summary::-webkit-details-marker {
    display: none;
  }

  .faq-summary-content {
    display: flex;
    align-items: center;
    gap: var(--space-5);
    flex: 1;
  }

  .faq-number {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--text-quaternary);
    font-family: var(--font-display);
    letter-spacing: 0.08em;
    transition: color 0.3s ease, transform 0.3s var(--ease-ios-spring);
    min-width: 28px;
  }

  .faq-item.is-open .faq-number {
    color: var(--color-primary);
  }

  .faq-question {
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.5;
    flex: 1;
    transition: color 0.3s;
  }

  .faq-item:hover .faq-question {
    color: var(--color-primary);
  }

  .faq-icon-btn {
    flex-shrink: 0;
    position: relative;
  }

  .faq-icon-bg {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--bg-tertiary);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.4s ease, transform 0.4s var(--ease-ios-spring);
  }

  .faq-chevron {
    color: var(--text-tertiary);
    transition: transform 0.4s var(--ease-ios-spring), color 0.4s ease;
  }

  .faq-item.is-open .faq-icon-bg {
    background: var(--color-primary);
  }

  .faq-item.is-open .faq-chevron {
    transform: rotate(180deg);
    color: white;
    stroke: white;
  }

  /* FAQ Body Animation — grid-template-rows for buttery smooth height */
  .faq-body {
    display: grid;
    grid-template-rows: 0fr;
    opacity: 0;
    transition: grid-template-rows 0.5s var(--ease-ios-decel), opacity 0.35s ease;
  }

  .faq-body-inner {
    overflow: hidden;
    padding: 0 var(--space-6) 0 76px;
  }

  .faq-item.is-open .faq-body {
    grid-template-rows: 1fr;
    opacity: 1;
  }

  .faq-answer {
    transform: translateY(-6px);
    opacity: 0;
    transition: transform 0.45s var(--ease-ios-decel) 0.05s, opacity 0.35s ease 0.05s;
  }

  .faq-answer p {
    font-size: var(--text-base);
    line-height: 1.7;
    color: var(--text-secondary);
    padding-bottom: var(--space-6);
  }

  .faq-item.is-open .faq-answer {
    transform: translateY(0);
    opacity: 1;
  }

  /* ========== Contact Card (Premium Liquid Glass) ========== */
  .faq-contact-col {
    position: relative;
  }

  .contact-card {
    position: sticky;
    top: 100px;
    background: var(--glass-bg);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-glass);
    overflow: hidden;
    transition: transform 0.45s var(--ease-ios-spring), box-shadow 0.35s var(--ease-ios), background 0.35s var(--ease-ios);
    transform: translateZ(0);
    will-change: transform;
  }

  .contact-card::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 1;
    border-radius: inherit;
    background:
      linear-gradient(120deg, transparent 0%, rgba(255,255,255,0.20) 20%, transparent 38%),
      radial-gradient(ellipse 78% 46% at 50% -10%, rgba(0,122,255,0.10), transparent 72%);
    opacity: 0;
    transform: translateX(-14%);
    pointer-events: none;
    transition: opacity 0.35s var(--ease-ios), transform 0.7s var(--ease-ios-decel);
  }

  .contact-specular {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 55%;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.92) 0%,
      rgba(255, 255, 255, 0.45) 35%,
      rgba(255, 255, 255, 0.12) 65%,
      transparent 100%
    );
    pointer-events: none;
    z-index: 1;
  }

  .contact-card-content {
    position: relative;
    z-index: 2;
    padding: 44px 36px var(--space-10);
  }

  .contact-card:hover {
    transform: translateY(-4px) scale(1.003);
    background: var(--glass-bg-hover);
    box-shadow: var(--shadow-glass-hover);
  }

  .contact-card:hover::after {
    opacity: 0.74;
    transform: translateX(0);
  }

  .contact-card:hover .contact-avatar {
    transform: scale(1.08);
  }

  /* Avatar */
  .contact-avatar-wrap {
    display: flex;
    justify-content: center;
    margin-bottom: var(--space-6);
    position: relative;
  }

  .contact-avatar {
    position: relative;
    width: 96px;
    height: 96px;
    border-radius: 50%;
    padding: 4px;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    box-shadow: 0 8px 32px color-mix(in srgb, var(--color-primary) 35%, transparent);
    transition: transform 0.4s var(--ease-ios-spring), box-shadow 0.4s ease;
  }

  .contact-card:hover .contact-avatar {
    transform: scale(1.05);
    box-shadow: 0 12px 40px color-mix(in srgb, var(--color-primary) 45%, transparent);
  }

  .contact-avatar img {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid white;
  }

  .contact-status {
    position: absolute;
    bottom: 0;
    right: calc(50% - 44px);
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: var(--color-success);
    border: 3px solid white;
  }

  .status-ping {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    background: var(--color-success);
    animation: status-pulse 2s ease-out infinite;
  }

  @keyframes status-pulse {
    0% { 
      transform: scale(1);
      opacity: 0.8;
    }
    100% { 
      transform: scale(2.5);
      opacity: 0;
    }
  }

  /* Card Text */
  .contact-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    color: var(--text-primary);
    text-align: center;
    margin-bottom: 10px;
  }

  .contact-description {
    font-size: var(--text-md);
    line-height: 1.6;
    color: var(--text-secondary);
    text-align: center;
    margin-bottom: var(--space-8);
  }

  /* Contact Actions */
  .contact-actions {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    margin-bottom: var(--space-8);
  }

  :global(.contact-cta) {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    width: 100%;
    padding: 16px var(--space-6);
    border-radius: var(--radius-xl);
    font-size: var(--text-md);
    font-weight: 600;
    text-decoration: none;
    transition: transform 0.35s var(--ease-ios-spring), background 0.3s var(--ease-ios), color 0.3s var(--ease-ios), border-color 0.3s var(--ease-ios), box-shadow 0.3s var(--ease-ios);
    overflow: hidden;
  }

  :global(.contact-cta-primary) {
    background: var(--color-primary);
    color: white;
    box-shadow: 0 4px 16px color-mix(in srgb, var(--color-primary) 40%, transparent);
  }

  :global(.contact-card .cta-bg) {
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.25), transparent);
    background-size: 200% 100%;
    animation: shimmer 3s ease-in-out infinite;
  }

  @keyframes shimmer {
    0%, 100% { background-position: 200% 0; }
    50% { background-position: -200% 0; }
  }

  :global(.cta-content) {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  :global(.contact-cta-primary:hover) {
    transform: translateY(-2px) scale(1.02);
    box-shadow: 0 8px 28px color-mix(in srgb, var(--color-primary) 50%, transparent);
  }

  :global(.contact-cta-secondary) {
    background: rgba(0, 0, 0, 0.04);
    color: var(--text-primary);
    border: 1px solid rgba(0, 0, 0, 0.06);
  }

  :global(.contact-cta-secondary:hover) {
    background: color-mix(in srgb, var(--color-primary) 10%, transparent);
    color: var(--color-primary);
    border-color: color-mix(in srgb, var(--color-primary) 20%, transparent);
    transform: translateY(-2px);
  }

  /* Trust Indicators */
  .contact-trust {
    border-top: 1px solid var(--separator);
    padding-top: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    color: var(--color-success);
  }

  .trust-icon {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: color-mix(in srgb, var(--color-success) 10%, transparent);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .trust-item span {
    color: var(--text-secondary);
  }

  /* ========== Responsive ========== */
  @media (max-width: 1024px) {
    .faq-layout {
      grid-template-columns: 1fr;
      gap: var(--space-16);
    }

    .contact-card {
      position: static;
      max-width: 480px;
      margin: 0 auto;
    }
  }

  @media (max-width: 768px) {
    .faq-section {
      padding: var(--section-py-mobile) 0;
    }

    .faq-summary {
      padding: var(--space-5);
    }

    .faq-summary-content {
      gap: var(--space-3);
    }

    .faq-question {
      font-size: var(--text-lg);
    }

    .faq-body-inner {
      padding: 0 var(--space-5) var(--space-5) 58px;
    }
  }

  @media (max-width: 480px) {
    .faq-body-inner {
      padding: 0 var(--space-4) var(--space-4) 48px;
    }

    .faq-summary {
      padding: var(--space-4);
      gap: var(--space-2);
    }

    .faq-summary-content {
      gap: var(--space-2);
    }

    .faq-question {
      font-size: var(--text-base);
    }

    .faq-number {
      font-size: var(--text-xs);
      min-width: 22px;
    }

    .contact-card-content {
      padding: 36px var(--space-6) var(--space-8);
    }

    .contact-avatar {
      width: 84px;
      height: 84px;
    }
  }

  @media (max-width: 480px) {
    .faq-number {
      font-size: var(--text-xs);
      min-width: 24px;
    }

    .faq-icon-bg {
      width: 32px;
      height: 32px;
    }

    .faq-chevron {
      width: 14px;
      height: 14px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .faq-item {
      transition: background 0.2s ease !important;
    }

    .faq-item::after,
    .contact-card::after,
    .faq-body,
    .faq-answer,
    .faq-icon-bg,
    .faq-chevron {
      transition: none !important;
    }

    .contact-card:hover {
      transform: none;
    }

    .contact-avatar,
    :global(.contact-card .cta-bg) {
      animation: none !important;
    }

    .status-ping {
      animation: none !important;
    }

    .faq-item.is-open .faq-body {
      grid-template-rows: 1fr;
      opacity: 1;
    }

    .faq-answer {
      transform: translateY(0);
      opacity: 1;
    }
  }
</style>

<script>
  // FAQ Accordion — smooth grid-template-rows animation
  document.querySelectorAll('.faq-item').forEach((item, index) => {
    const summary = item.querySelector('.faq-summary');
    const body = item.querySelector('.faq-body');

    // Staggered entrance animation
    item.style.opacity = '0';
    item.style.transform = 'translateY(30px) scale(0.98)';
    setTimeout(() => {
      item.style.transition = 'opacity 0.6s var(--ease-ios-decel), transform 0.6s var(--ease-ios-spring), background 0.35s var(--ease-ios), box-shadow 0.35s var(--ease-ios)';
      item.style.opacity = '1';
      item.style.transform = 'translateY(0) scale(1)';
    }, index * 100);

    summary?.addEventListener('click', (e) => {
      e.preventDefault();
      const isOpen = item.classList.contains('is-open');

      // Close other items
      document.querySelectorAll('.faq-item.is-open').forEach(openItem => {
        if (openItem !== item) {
          openItem.classList.remove('is-open');
          // Remove [open] after CSS transition finishes
          const onTransitionEnd = (ev) => {
            if (ev.target === openItem.querySelector('.faq-body')) {
              openItem.removeAttribute('open');
              openItem.removeEventListener('transitionend', onTransitionEnd);
            }
          };
          openItem.addEventListener('transitionend', onTransitionEnd);
          // Fallback: remove after 550ms if transitionend doesn't fire
          setTimeout(() => {
            openItem.removeAttribute('open');
            openItem.removeEventListener('transitionend', onTransitionEnd);
          }, 550);
        }
      });

      if (isOpen) {
        item.classList.remove('is-open');
        const onTransitionEnd = (ev) => {
          if (ev.target === body) {
            item.removeAttribute('open');
            item.removeEventListener('transitionend', onTransitionEnd);
          }
        };
        item.addEventListener('transitionend', onTransitionEnd);
        setTimeout(() => {
          item.removeAttribute('open');
          item.removeEventListener('transitionend', onTransitionEnd);
        }, 550);
      } else {
        item.setAttribute('open', '');
        requestAnimationFrame(() => {
          requestAnimationFrame(() => {
            item.classList.add('is-open');
          });
        });
      }
    });
  });

  // Contact card 3D tilt effect (desktop only)
  const contactCard = document.querySelector('[data-contact-card]');
  if (contactCard && !window.matchMedia('(pointer: coarse)').matches) {
    contactCard.addEventListener('mousemove', (e) => {
      const rect = contactCard.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;
      const centerX = rect.width / 2;
      const centerY = rect.height / 2;

      const rotateX = (y - centerY) / centerY * -4;
      const rotateY = (x - centerX) / centerX * 4;

      (contactCard).style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateY(-4px)`;
    });

    contactCard.addEventListener('mouseleave', () => {
      (contactCard).style.transform = '';
    });
  }
</script>
